{
  "name": "pi-fallback-models",
  "version": "0.1.1",
  "description": "A Pi Coding Agent extension that tries configured fallback models in order after eligible failures, with session, directory, and global settings.",
  "author": {
    "name": "dev-willbird1936",
    "url": "https://github.com/dev-willbird1936"
  },
  "license": "MIT",
  "keywords": ["pi-package", "pi-extension", "fallback", "models"],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dev-willbird1936/pi-fallback-models.git"
  },
  "bugs": {
    "url": "https://github.com/dev-willbird1936/pi-fallback-models/issues"
  },
  "homepage": "https://github.com/dev-willbird1936/pi-fallback-models#readme",
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "files": ["src", "README.md", "LICENSE", "CHANGELOG.md", "settings.bat", "tools"],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "check": "npm run typecheck && npm test && node src/extension.ts --selfcheck"
  },
  "pi": {
    "extensions": ["./src/extension.ts"]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@types/node": "^22.19.0",
    "typescript": "^5.9.0"
  }
}
